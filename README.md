# Brute-Force-Protection-System
This system monitors login attempts and temporarily locks out identifiers (IP addresses or usernames) that exceed the allowed number of failed attempts within a specific time window.
